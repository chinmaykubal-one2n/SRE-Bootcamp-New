apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: student-api-ingress
  namespace: student-api
spec:
  rules:
    - host: students-api.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: students-api-service
                port:
                  number: 3000